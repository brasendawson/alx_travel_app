# Django Settings
SECRET_KEY=django-insecure-placeholder-key-for-development
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Set USE_MYSQL=True to use MySQL, False to use SQLite for development
USE_MYSQL=False

# MySQL Configuration (only used if USE_MYSQL=True)
DB_NAME=alx_travel_app
DB_USER=root
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3306

# Other settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000